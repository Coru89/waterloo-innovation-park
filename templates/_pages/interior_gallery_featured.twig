{# setting the base layout based on if ajax request #}
{% set theLayout = craft.app.request.isAjax and not craft.app.request.isLivePreview ? 'ajaxLayout' : 'base' %}
{% extends theLayout %}

{% set displayLimit = 4 %}
{% set entries = craft.entries.section('exhibitions').one().galleryItems.limit(displayLimit) %}

{% set body_class = 'page-interior' %}

{% block layout %}
	{% include '_components/page-intro' %}
	{% include '_components/featured-exhibit-rollup' %}
{% endblock %}
